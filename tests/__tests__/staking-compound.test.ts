import { buildAutoCompoundInvocation, projectCompoundYield } from '@/lib/staking/compound';
describe('auto-compound projections', () => { it('projects a higher compounded yield', () => { const projection = projectCompoundYield(100, 0.12, 12); expect(projection.compoundedYield).toBeGreaterThan(projection.standardYield); expect(projection.apy).toBeGreaterThan(0.12); }); it('builds one harvest-and-stake invocation', () => { expect(buildAutoCompoundInvocation('pool', 'GACCOUNT', 'usdc')).toEqual({ contractId: 'pool', method: 'harvest_and_stake', args: ['GACCOUNT', 'usdc'] }); }); });
